name: new-linux

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    

    steps:
      - name: Downloading & Setting Up
        run: |
         sudo apt update
         sudo apt install tmate -y
         sudo tmate
